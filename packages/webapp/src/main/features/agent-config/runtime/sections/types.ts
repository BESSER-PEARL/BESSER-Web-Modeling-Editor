import type { AgentRuntimeForm } from '../useAgentRuntimeForm';

/** Props shared by the config-file sections of the agent runtime panel. */
export interface ConfigSectionProps {
  runtime: AgentRuntimeForm;
}
