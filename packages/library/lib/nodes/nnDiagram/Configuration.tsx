import { makeNNLayerComponent } from "./_NNLayerBase"

/** SA-5 `Configuration` — model-level training config. Same flat
 * `attributes` shape (no `name` slug; uses dedicated config slugs). */
export const Configuration = makeNNLayerComponent(
  "Configuration",
  "Configuration",
  "#FCE4EC"
)
